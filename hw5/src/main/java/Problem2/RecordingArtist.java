package Problem2;

public class RecordingArtist extends Person{

  /**
   *
   * @param firstName recordingArtist's firstName expressed as String
   * @param lastName recordingArtist's lastName expressed as String
   */
  public RecordingArtist(String firstName, String lastName) {
    super(firstName, lastName);
  }
}
